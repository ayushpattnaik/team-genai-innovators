# This file makes 'twitter' a Python package.
# It should import the agent instance to make it discoverable.

# Import the agent instance (using conventional 'city_agent' name)
from .agent import root_agent 